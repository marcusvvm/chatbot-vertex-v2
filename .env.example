# ========================================
# RAG API - Configuração de Ambiente
# ========================================
# 
# Copie este arquivo e renomeie para .env:
#   cp .env.example .env
#
# Depois edite apenas a seção JWT (abaixo)
#
# 📚 Documentação: docs/ARCHITECTURE.md
#
# ========================================

# ========================================
# 1. GOOGLE CLOUD PLATFORM (GCP)
# ========================================
# ✅ Já configurado - não precisa alterar

GOOGLE_APPLICATION_CREDENTIALS=credentials/credentials-rag.json
GCP_PROJECT_ID=rag-projetos-crea
GCP_LOCATION=europe-west3
GCP_LOCATION_CHAT=us-central1

# ========================================
# 2. CONFIGURAÇÃO DA API
# ========================================
# ✅ Já configurado - não precisa alterar

API_TITLE=RAG API
API_VERSION=1.0.0
API_PREFIX=/api/v1

# Modo debug (ativa logs detalhados)
# ⚠️ Altere para False em produção!
DEBUG=False

# ========================================
# 3. AUTENTICAÇÃO JWT
# ========================================
# ⚠️ VOCÊ PRECISA CONFIGURAR ESTA SEÇÃO!

# Gere uma chave secreta forte:
#   openssl rand -hex 32
# 
# ⚠️ NUNCA use a chave de exemplo abaixo em produção!
JWT_SECRET_KEY=ALTERE-ESTA-CHAVE-SECRETA-MINIMO-32-CARACTERES

# Não altere (padrão HS256)
JWT_ALGORITHM=HS256

# Tempo de expiração em horas:
#   Desenvolvimento: 24 (1 dia)
#   Produção: 175200 (20 anos)
JWT_EXPIRATION_HOURS=720

# ========================================
# ✅ PRÓXIMOS PASSOS
# ========================================
#
# 1. cp .env.example .env
#
# 2. Gere uma chave JWT forte:
#    openssl rand -hex 32
#
# 3. Edite .env e substitua JWT_SECRET_KEY
#
# 4. Gere um token:
#    python scripts/generate_token.py --user admin --purpose admin
#
# 5. Inicie o servidor:
#    ./venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
#
# 6. Acesse: http://localhost:8000/docs
#
# ========================================
